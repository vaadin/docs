extends: conditional
message: "'%s' has no definition."
link: https://vaadin.com/docs/latest/contributing-docs/style-guide/grammar/#abbreviations
level: warning
scope: text
ignorecase: false
# Ensures that the existence of 'first' implies the existence of 'second'.
first: '\b([A-Z]{3,5})\b'
second: '(?:\b[A-Z][a-z]+ )+\(([A-Z]{3,5})\)'
# ... with the exception of these:
exceptions:
  - API
  - SAP
  - ASCII
  - CLI
  - CPU
  - CRUD
  - CSS
  - CSV
  - DOM
  - DPI
  - FAQ
  - GET
  - GIF
  - GPU
  - GUI
  - HTML
  - HTTP
  - HTTPS
  - IDE
  - JAR
  - JPA
  - JSON
  - JSX
  - JVM
  - JWT
  - LDAP
  - MVC
  - PATH
  - PDF
  - PNG
  - POST
  - SDK
  - SQL
  - SSH
  - SSL
  - SVG
  - TCP
  - REST
  - URI
  - URL
  - USB
  - UTF
  - WAR
  - XML
  - YAML
  - ZIP
