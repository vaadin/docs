name: Check Stale Version References

on:
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Check stale version references
        run: node scripts/check-stale-version-references.js
