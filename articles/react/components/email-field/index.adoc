---
title: Email Field
description: An extension of Text Field, this field only accepts email addresses as input.
---


= Email Field

If the given address is invalid, the field is highlighted in red and an error message appears underneath the input.

[source,tsx]
----
<HorizontalLayout theme="spacing">
  <EmailField
    label="Email address"
    value="julia.scheider@example.com"
    errorMessage="Enter a valid email address"
    clearButtonVisible
  />
  <EmailField
    label="Email address"
    value="This is not an email"
    errorMessage="Enter a valid email address"
    clearButtonVisible
    invalid
  />
</HorizontalLayout>
----

// TODO: define {article} and fix relative link
:component-name: Email Field
:text-field-features: true
include::../_shared.asciidoc[tag=field-features]


== Pattern for Additional Validation

The pattern attribute is an additional validation criterion. You might set this, for example, if you want to require a specific domain. The pattern is specified using a regular expression.

The example below uses the pattern `.+@example\.com` and only accepts addresses in the `example.com` domain:

[source,tsx]
----
<EmailField
  label="Email address"
  value="julia@example.com"
  errorMessage="Enter a valid email address"
  pattern="^.+@example\.com$"
/>
----

:component-path: email-field
include::{articles}/react/components/_vaadindocs.adoc[]
