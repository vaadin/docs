# TODO: The reverse proxy rules doesn't currently work because the
# location rules in gatsbyjs/gatsby image override them
# https://github.com/gatsbyjs/gatsby-docker/blob/master/nginx-boot.sh#L98

location /vaadin/ {
    proxy_pass http://docs:8080/vaadin;
}

location /connect/ {
    proxy_pass http://docs:8080/connect;
}
