import { css } from 'lit';

const styles = css`
  #container {
    align-items: stretch;
    border: 1px solid var(--vaadin-border-color);
    max-width: 100%;
    height: 400px;
    width: 360px;
  }
`;

export default styles;
